---
phase: 01-skill-router
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL.md
  - modes/audit.md
  - modes/checklist.md
  - modes/preventive.md
  - vectors/.gitkeep
  - stacks/.gitkeep
  - references/.gitkeep
  - scripts/.gitkeep
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03]

must_haves:
  truths:
    - "Claude Code discovers rock-house when user types /rock-house or says 'audit this project'"
    - "SKILL.md detects Node.js stack from package.json, Python stack from requirements.txt, static web from .html files"
    - "SKILL.md routes to the correct mode file based on user intent (audit, checklist, preventive)"
    - "Only the selected mode file is loaded — not all sub-modules at once"
    - "SKILL.md body stays under 150 lines"
  artifacts:
    - path: "SKILL.md"
      provides: "Skill entry point with stack detection, mode routing, and progressive disclosure"
      contains: "name: rock-house"
    - path: "modes/audit.md"
      provides: "Stub for audit mode (expanded in Phase 2)"
      contains: "Audit Mode"
    - path: "modes/checklist.md"
      provides: "Stub for checklist mode (expanded in Phase 7)"
      contains: "Checklist Mode"
    - path: "modes/preventive.md"
      provides: "Stub for preventive mode (expanded in Phase 8)"
      contains: "Preventive Mode"
  key_links:
    - from: "SKILL.md"
      to: "modes/audit.md"
      via: "markdown file reference"
      pattern: "modes/audit.md"
    - from: "SKILL.md"
      to: "modes/checklist.md"
      via: "markdown file reference"
      pattern: "modes/checklist.md"
    - from: "SKILL.md"
      to: "modes/preventive.md"
      via: "markdown file reference"
      pattern: "modes/preventive.md"
---

<objective>
Create the SKILL.md entry point for Rock House — the file Claude Code reads to activate the security skill — along with the directory structure and mode stubs that enable progressive disclosure.

Purpose: This is the foundation of Rock House. Without SKILL.md, none of the subsequent phases (audit mode, vectors, stacks, scripts, score) can function. SKILL.md must detect the project stack automatically (CORE-01), route to the correct mode (CORE-02), and load only relevant modules (CORE-03).

Output: Working SKILL.md (<150 lines) with YAML frontmatter, stack detection instructions, mode routing table, and file references to sub-modules. Mode stub files in modes/ directory. Empty directories for vectors/, stacks/, references/, scripts/.
</objective>

<execution_context>
Read this plan carefully. Execute each task in order. After all tasks, create the SUMMARY.md file.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-skill-router/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory structure and mode stubs</name>
  <files>modes/audit.md, modes/checklist.md, modes/preventive.md, vectors/.gitkeep, stacks/.gitkeep, references/.gitkeep, scripts/.gitkeep</files>
  <action>
Create the Rock House directory structure at the repo root (C:\Users\ANGELO SILVA\Documents\rock-house\). This structure follows the architecture from RESEARCH.md and ARCHITECTURE.md.

Create four directories: modes/, vectors/, stacks/, references/, scripts/. For vectors/, stacks/, references/, and scripts/, create a .gitkeep file so git tracks the empty directories.

Create three mode stub files. These are NOT placeholders — they are functional stubs that tell Claude what the mode does and that the full implementation is coming in a later phase. Each stub should be 15-25 lines and contain:
1. A heading with the mode name
2. A brief description of what this mode does (in English, per project constraint)
3. A "Status" note saying implementation is in Phase N
4. A "When loaded" section explaining what inputs the mode expects from SKILL.md (detected stacks, user request context)

modes/audit.md content:
- Heading: "# Audit Mode"
- Purpose: "Scans existing project code against security attack vectors and generates a severity-graded report."
- What it does: reads project files, applies vector detection rules for the detected stack, runs kill-chain analysis testing each defense layer independently, produces findings with severity (Critical/High/Medium/Low), file:line, description, impact, and fix suggestions
- Report language: Portuguese (PT-BR)
- Vectors it will load: list all 5 vector file references (vectors/injection.md, vectors/auth-access.md, vectors/secrets-exposure.md, vectors/upload-network.md, vectors/supply-chain.md)
- Stack rules it will load: reference to stacks/ directory based on detected stack
- Status: "Full implementation in Phase 2 (Audit Mode Foundation)"

modes/checklist.md content:
- Heading: "# Checklist Mode"
- Purpose: "Pre-deploy gate that validates defense-in-depth across all relevant risks before shipping."
- What it does: presents items organized by risk category (not by technology), requires at least 2 independent defense layers per risk to pass, filters to max ~30 items relevant to detected stack, marks each pass/fail/warning
- Status: "Full implementation in Phase 7 (Checklist Mode)"

modes/preventive.md content:
- Heading: "# Preventive Mode"
- Purpose: "Generates defense-in-depth planning tables BEFORE writing code for a new feature."
- What it does: user describes a feature type (auth, form, api, payment, upload, etc.), mode identifies applicable risks, generates a defense table showing at least 2 independent layers per risk with code examples in the project's detected stack
- Status: "Full implementation in Phase 8 (Preventive Mode)"
  </action>
  <verify>
    <automated>ls modes/audit.md modes/checklist.md modes/preventive.md vectors/.gitkeep stacks/.gitkeep references/.gitkeep scripts/.gitkeep</automated>
  </verify>
  <done>All 4 directories exist (modes/, vectors/, stacks/, references/, scripts/). Three mode stubs exist with proper content describing their purpose and expected inputs. Empty directories have .gitkeep files.</done>
</task>

<task type="auto">
  <name>Task 2: Create SKILL.md with frontmatter, stack detection, mode routing, and progressive disclosure</name>
  <files>SKILL.md</files>
  <action>
Create SKILL.md at the repo root (C:\Users\ANGELO SILVA\Documents\rock-house\SKILL.md). This is the most critical file in the entire project. It must stay under 150 lines total. Follow the Agent Skills specification (agentskills.io) and Claude Code skills documentation (code.claude.com/docs/en/skills) exactly.

YAML frontmatter (lines 1-7):
- name: rock-house (lowercase + hyphens only, must match directory name)
- description: Must say WHEN to use, never HOW. Write in third person. Front-load keywords. Keep concise because ~90 other skills compete for the description token budget. Use this exact text: "Security audit and defense-in-depth analysis for web projects. Use when the user asks to audit code security, check for vulnerabilities, review OWASP compliance, run a pre-deploy security checklist, or plan defenses before coding a new feature."
- when_to_use: Secondary trigger phrases. Use: "When user mentions: security audit, vulnerability scan, check security, OWASP, security headers, secrets in code, defense in depth, pre-deploy review, rock house, casa de pedra."
- allowed-tools: "Read Glob Grep" (only tools needed for static analysis; Bash commands for scripts will be added in Phase 10)

Body structure (aim for ~120-140 lines total including frontmatter):

Section 1 — "# Rock House - Security Defense in Depth" (1 line)
Brief intro (2 lines max): "Analyzes web projects for security vulnerabilities using independent defense layers. Each layer must hold on its own — if one falls, the next stops the attack."

Section 2 — "## Step 1: Detect Project Stack" (~20 lines)
Instructions telling Claude what to look for in the project root. Use numbered list:
1. Check if package.json exists. If yes, read it and look for dependencies: next, react, express, firebase, supabase. Record as "Node.js" plus specific frameworks found.
2. Check if requirements.txt, pyproject.toml, or Pipfile exists. If yes, look for flask, django, fastapi. Record as "Python" plus specific frameworks.
3. Check if .html files exist in root or public/ directory. Record as "Static HTML/CSS/JS".
4. Check if supabase/ directory exists or .env contains SUPABASE keys. Record as "Supabase" (adds to existing stack, does not replace).
5. Check if vercel.json or next.config.* exists. Record as "Vercel/Next.js deployment".
6. If none detected, ask the user what stack they are using.
Note: multiple stacks can coexist (e.g., Next.js + Supabase). Record ALL detected stacks.

Section 3 — "## Step 2: Select Mode" (~15 lines)
Table mapping user intent to mode file:

| User Intent | Mode | Load |
|---|---|---|
| "audit", "scan", "check security", "review code", "find vulnerabilities" | Audit | Read [modes/audit.md](modes/audit.md) |
| "checklist", "pre-deploy", "ready to ship?", "deploy check" | Checklist | Read [modes/checklist.md](modes/checklist.md) |
| "planning", "new feature", "before I code", "defense table" | Preventive | Read [modes/preventive.md](modes/preventive.md) |

Add instruction: "If the user's intent is unclear, ask which mode they want. Show the three options with a one-line description of each."

Section 4 — "## Step 3: Load Relevant Modules" (~25 lines)
Instructions for progressive loading based on mode and stack. This section implements CORE-03.

Sub-section "### Vector Modules" — table with 5 rows:
| Module | Covers | File |
|---|---|---|
| Injection | XSS, SQLi, CSRF, Prompt Injection | [vectors/injection.md](vectors/injection.md) |
| Auth and Access | RLS Bypass, IDOR, JWT, Mass Assignment | [vectors/auth-access.md](vectors/auth-access.md) |
| Secrets and Exposure | Hardcoded keys, Security Headers, CORS | [vectors/secrets-exposure.md](vectors/secrets-exposure.md) |
| Upload and Network | SSRF, File Upload, Open Redirect | [vectors/upload-network.md](vectors/upload-network.md) |
| Supply Chain | Dependency vulnerabilities (npm/pip) | [vectors/supply-chain.md](vectors/supply-chain.md) |

Sub-section "### Stack-Specific Rules" — table with 5 rows:
| Stack Detected | File |
|---|---|
| HTML/CSS/JS | [stacks/html-js.md](stacks/html-js.md) |
| Next.js / React | [stacks/nextjs.md](stacks/nextjs.md) |
| Supabase / PostgreSQL | [stacks/supabase.md](stacks/supabase.md) |
| Flask / Python | [stacks/flask.md](stacks/flask.md) |
| Infrastructure / Deploy | [stacks/infra.md](stacks/infra.md) |

Instruction: "Load ONLY the vector modules and stack rules relevant to the detected stack and selected mode. Do NOT load all files. The mode file specifies which vectors to check."

Section 5 — "## Reporting Rules" (~10 lines)
- All reports MUST be in Portuguese (PT-BR)
- All findings MUST include: severity level (Critico/Alto/Medio/Baixo), file path and line number, description of the vulnerability, impact if exploited, current defense (if any), recommended fix with code example
- Use the metaphor: palha (straw) = no defense, madeira (wood) = partial defense, pedra (stone) = solid defense, fortaleza (fortress) = defense in depth with independent layers
- Group findings by attack vector, not by file

Section 6 — "## References (loaded on demand)" (~10 lines)
Table with 4 rows pointing to reference files:
| Reference | File |
|---|---|
| OWASP Top 10 (PT-BR) | [references/owasp-top10.md](references/owasp-top10.md) |
| OWASP API Top 10 (PT-BR) | [references/owasp-api.md](references/owasp-api.md) |
| Defense in Depth theory | [references/defense-in-depth.md](references/defense-in-depth.md) |
| Operational Defense guide | [references/operational.md](references/operational.md) |

Instruction: "Load a reference only when you find a specific vulnerability and need to cite the OWASP category or explain the defense theory."

CRITICAL ANTI-PATTERNS TO AVOID:
- Do NOT put workflow steps in the description field (causes Claude shortcutting)
- Do NOT use backslash paths (use forward slashes only)
- Do NOT use @ imports for sub-files (forces immediate loading, wastes tokens)
- Keep the most critical instructions (stack detection, mode routing) at the TOP of the body — after compaction, only the first ~5000 tokens are retained
- Do NOT exceed 150 lines total
  </action>
  <verify>
    <automated>wc -l SKILL.md | awk '{print ($1 <= 150) ? "PASS: " $1 " lines" : "FAIL: " $1 " lines (max 150)"}' && head -10 SKILL.md && grep -c 'modes/audit.md' SKILL.md && grep -c 'vectors/injection.md' SKILL.md</automated>
  </verify>
  <done>SKILL.md exists at repo root with: valid YAML frontmatter (name: rock-house, description with WHEN not HOW, when_to_use with trigger phrases, allowed-tools), stack detection instructions for package.json/requirements.txt/.html, mode routing table pointing to 3 mode files, vector module table pointing to 5 vector files, stack rules table pointing to 5 stack files, reporting rules in PT-BR, reference table pointing to 4 reference files. Total line count is 150 or fewer.</done>
</task>

<task type="auto">
  <name>Task 3: Set up symlink and verify skill discovery</name>
  <files>none (system configuration)</files>
  <action>
Set up the junction/symlink so Claude Code discovers rock-house as a global skill.

First, check if the skills directory exists at C:\Users\ANGELO SILVA\.claude\skills\. Create it if it does not exist.

Then check if C:\Users\ANGELO SILVA\.claude\skills\rock-house\ already exists. If it does, check what it contains. If it contains artifact files (numbered files like 10930, 11439, etc.) or is a broken junction, remove it first.

Create a Windows junction (not symlink — junctions do not require admin rights):
cmd /c mklink /J "C:\Users\ANGELO SILVA\.claude\skills\rock-house" "C:\Users\ANGELO SILVA\Documents\rock-house"

Verify the junction works by listing the target directory through the junction path and confirming SKILL.md is visible:
ls "C:\Users\ANGELO SILVA\.claude\skills\rock-house\SKILL.md"

Also verify the SKILL.md frontmatter is valid by checking:
1. name field is "rock-house" (lowercase, hyphens only)
2. description field exists and does NOT contain workflow steps (no "scans code", "generates report" — only "Use when..." type language)
3. File is under 150 lines

Final verification: count the total lines in SKILL.md and confirm the description says WHEN to use, not HOW.
  </action>
  <verify>
    <automated>ls 'C:/Users/ANGELO SILVA/.claude/skills/rock-house/SKILL.md' && head -6 'C:/Users/ANGELO SILVA/.claude/skills/rock-house/SKILL.md' && wc -l 'C:/Users/ANGELO SILVA/.claude/skills/rock-house/SKILL.md'</automated>
  </verify>
  <done>Junction exists from ~/.claude/skills/rock-house/ pointing to ~/Documents/rock-house/. SKILL.md is discoverable through the junction path. Frontmatter name is "rock-house", description describes WHEN to use the skill. File is under 150 lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill file content | SKILL.md is loaded into Claude's context — malicious edits would alter behavior |
| allowed-tools grant | Tools listed in frontmatter get permission-free execution |
| Dynamic context injection | Shell commands via ! syntax run with user's permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | SKILL.md body | accept | Skill files have same trust level as CLAUDE.md — local files, user-controlled. Git tracks changes. |
| T-01-02 | Elevation of Privilege | allowed-tools in frontmatter | mitigate | Only grant Read, Glob, Grep (read-only tools). Do NOT grant Bash broadly. Specific Bash commands for scripts will be added in Phase 10 with explicit patterns. |
| T-01-03 | Information Disclosure | Stack detection via file reads | accept | Reading package.json/requirements.txt is necessary for functionality and only accesses project-local files. |
| T-01-04 | Tampering | Junction/symlink | accept | Junction is local system config, same trust as any user-created directory. |
</threat_model>

<verification>
After all tasks complete, verify the full Phase 1 deliverable:

1. SKILL.md exists at repo root and is under 150 lines
2. SKILL.md frontmatter has name: rock-house, description (WHEN not HOW), when_to_use, allowed-tools
3. SKILL.md body has stack detection (package.json, requirements.txt, .html), mode routing (3 modes), vector references (5 vectors), stack references (5 stacks), reporting rules (PT-BR), reference links (4 refs)
4. modes/audit.md, modes/checklist.md, modes/preventive.md exist with functional stubs
5. vectors/, stacks/, references/, scripts/ directories exist
6. Junction from ~/.claude/skills/rock-house/ to ~/Documents/rock-house/ works
7. SKILL.md is visible through the junction path
</verification>

<success_criteria>
- SKILL.md is under 150 lines and contains all required sections
- Stack detection covers package.json (Node.js), requirements.txt (Python), .html (Static Web)
- Mode routing table maps user intent phrases to 3 mode files via markdown links
- Progressive disclosure is implemented: sub-modules are referenced (not @-imported), loaded only when the mode specifies
- Three mode stubs exist and describe their purpose and expected inputs
- Junction/symlink makes skill discoverable by Claude Code at ~/.claude/skills/rock-house/
- All file references use forward slashes, no backslash paths
- Description field says WHEN to use, not HOW (prevents Claude shortcutting)
</success_criteria>

<output>
Create .planning/phases/01-skill-router/01-01-SUMMARY.md when done
</output>
